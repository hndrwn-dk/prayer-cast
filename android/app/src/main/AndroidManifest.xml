<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Local network / NSD (presence + peer discovery).
         CHANGE_WIFI_MULTICAST_STATE also satisfies the official
         connectedDevice FGS runtime prerequisite (API 34+): at least
         one of CHANGE_NETWORK_STATE / CHANGE_WIFI_STATE /
         CHANGE_WIFI_MULTICAST_STATE / NFC / TRANSMIT_IR.
         WifiLock needs WAKE_LOCK only, not CHANGE_WIFI_STATE. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Optional location. Coarse fills city for prayer times when the
         user taps Use current location. Fine is for nearby mosques
         (current GPS). Not used to decide if the user is home (LAN
         fingerprint). Not used for speaker scan. Do not request on
         launch. City when traveling is changed manually on Prayer times. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Speaker scan on API 33+. neverForLocation: Wi-Fi is not a
         location source. Requested at scan time only.
         API 32 and below: ACCESS_WIFI_STATE + multicast; Scan does
         not prompt for location. -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <!-- Exact alarm — request at runtime. Do NOT add USE_EXACT_ALARM (spec §5.5). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Foreground service wake path for adzan delivery.
         AdzanForegroundService is connectedDevice (Wi-Fi lock + Dart
         start for Cast / LAN coordination; it does not play media).
         MEDIA_PLAYBACK stays for Cast SDK MediaNotificationService only. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Alarm-clock FGS must launch MainActivity while the app is backgrounded
         (Pixel / API 34+ BAL otherwise blocks startActivity; Dart never runs). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.TURN_SCREEN_ON" />

    <!-- Cleartext is off for the app process (targetSdk 28+ default, plus
         network-security-config). Cast still plays: MediaServer serves
         http://<LAN-IP>:<port>/... and the speaker fetches it; the phone
         never HttpClient-GETs that URL. Do not set usesCleartextTraffic
         true — Android cannot CIDR-whitelist RFC1918 in domain-config. -->
    <application
        android:label="Prayer Cast"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver
            android:name=".AdzanAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".PrePrayerAlertReceiver"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".NextPrayerWidget"
            android:exported="true"
            android:label="@string/next_prayer_widget_eyebrow">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_prayer_widget_info" />
        </receiver>

        <!-- Play-honest type: keep Wi-Fi up and start Dart so Cast
             discovery / election / loadMedia can talk to the home
             speaker over the LAN. Not mediaPlayback — this service
             never starts a player. Cast SDK MediaNotificationService
             below stays mediaPlayback. Play Console text: docs/play-fgs.md -->
        <service
            android:name=".AdzanForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Required by flutter_chrome_cast / Google Cast SDK. -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.felnanuke.google_cast.GoogleCastOptionsProvider" />

        <service
            android:name="com.google.android.gms.cast.framework.media.MediaNotificationService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for url_launcher to open Ko-fi / https links -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <!-- OEM Auto-launch / Startup Manager (Android 11+ package visibility).
             Component names are best-effort; queries only make resolve/start
             possible, they do not prove the activity exists. -->
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.oplus.safecenter" />
        <package android:name="com.oppo.safe" />
        <package android:name="com.oplus.battery" />
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.miui.powerkeeper" />
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.iqoo.secure" />
        <package android:name="com.oneplus.security" />
    </queries>
</manifest>
